# Data Source created from Pipe 'mat_stats_by_min'

SCHEMA >
    `t` DateTime,
    `player_id` String,
    `n_games` AggregateFunction(uniq, String),
    `scores` AggregateFunction(countIf, UInt8),
    `purchases` AggregateFunction(countIf, UInt8)

ENGINE "AggregatingMergeTree"
ENGINE_PARTITION_KEY "toYYYYMM(t)"
ENGINE_SORTING_KEY "player_id, t"
